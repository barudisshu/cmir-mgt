server {
  listen 80;
  server_name localhost;
  root /usr/share/nginx/html;
  index index.html index.htm;
  error_page 404 /index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location ~ ^/prod-api/ {
    proxy_pass http://cmir-mgt:8080/;
  }
}
